import { Component } from '@angular/core';

@Component({
  selector: 'app-root',  // Selector determines on which HTML element will ir be run
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.scss']
})
export class AppComponent {
}
